{
  "name": "documind",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm.cmd run dev --workspace backend",
    "dev:frontend": "npm.cmd run dev --workspace frontend",
    "dev:backend": "npm.cmd run dev --workspace backend",
    "build": "npm.cmd run build --workspace backend && npm.cmd run build --workspace frontend",
    "check": "npm.cmd run check --workspace backend && npm.cmd run check --workspace frontend"
  }
}
